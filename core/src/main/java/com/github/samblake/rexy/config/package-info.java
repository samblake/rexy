/**
 * <p>On startup the configuration will be loaded from the classpath, falling back to the filesystem if that fails.
 * The default path is `rexy.json`. A custom path can be supplied as a command line argument when running Rexy.</p>
 *
 * <p>Rexy contains examples for two APIs, the <a href="http://www.metaweather.com/api">metaweather API</a> and the
 * <a href="https://api.chucknorris.io">chucknorris.io API</a> (see <code>example-config.json</code>).</p>
 */
package com.github.samblake.rexy.config;